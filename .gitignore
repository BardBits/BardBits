node_modules
dist
dist-ssr
.site-preview
.DS_Store

# Machine-specific: absolute paths and per-user tool locations. launch.json is
# shared deliberately; this one is not.
.claude/settings.local.json

# Commercial positioning and strategy. This repository is public and the site
# does not say these things out loud, so the notes stay on the operator's
# machine. HANDOFF.md is the tracked, technical half and points at this file.
NOTES.md

# Generated by scripts/build-sitemap-index.mjs into the root project's source
# directory, because that is where the deploy script looks for files to publish.
projects/site-root/sitemap.xml
projects/site-root/pages.xml
